import React from "react";
import PropTypes from "prop-types";

const CardOnSite = ({ data, onChange }) => {
    return <div className="cardOnSite">cardOnSite</div>;
};
CardOnSite.propTypes = {
    data: PropTypes.object,
    onChange: PropTypes.func
};
export default CardOnSite;
